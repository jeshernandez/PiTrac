title: PiTrac - DIY Golf Launch Monitor
description: Complete documentation for PiTrac, the open-source DIY golf launch monitor built with Raspberry Pi. Get ball speed, launch angles, spin rates, and golf simulator integration.
theme: just-the-docs

url: https://jamespilgrim.github.io
baseurl: /PiTrac

# Logo
logo: "/assets/images/logos/PiTrac.svg"

aux_links:
  Edit Me: https://github.com/jamespilgrim/PiTrac/tree/main/docs

search_enabled: true

nav_enabled: true

heading_anchors: true

favicon_ico: "/assets/images/favicon.ico"

# For copy button on code
enable_copy_code_button: true

# Back to top link
back_to_top: true
back_to_top_text: "Back to top"

# Footer last edited timestamp
last_edit_timestamp: true # show or hide edit time - page must have `last_modified_date` defined in the frontmatter
last_edit_time_format: "%b %e %Y at %I:%M %p" # uses ruby's time format: https://ruby-doc.org/stdlib-2.7.0/libdoc/time/rdoc/Time.html

plugins:
- jekyll-seo-tag
- jekyll-sitemap
- jekyll-feed

# RSS feed
feed:
  excerpt_only: true

search:
  # Split pages into sections that can be searched individually
  # Supports 1 - 6, default: 2
  heading_level: 3
  # Maximum amount of previews per search result
  # Default: 3
  previews: 5
  # Maximum amount of words to display before a matched word in the preview
  # Default: 5
  preview_words_before: 8
  # Maximum amount of words to display after a matched word in the preview
  # Default: 10
  preview_words_after: 15
  # Set the search token separator - optimized for technical terms
  tokenizer_separator: /[\s\-_/\.]+/
  # Display the relative url in search results
  # Supports true (default) or false
  rel_url: true
  # Enable search button in bottom right corner for better UX
  button: true
  # Focus the search input by pressing `ctrl + focus_shortcut_key` (or `cmd + focus_shortcut_key` on macOS)
  focus_shortcut_key: 'k'
